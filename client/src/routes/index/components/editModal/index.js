import {
  Modal,
} from 'antd'

class EditModal extends React.Component {
  render() {
    // props definition
    const propsOfModal = {
      visible: false,
      title: '新增条目',
    }

    return (
      <Modal>
        content
      </Modal>
    )
  }
}

export default EditModal
